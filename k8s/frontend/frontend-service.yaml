apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: sentinelopsai
  labels:
    app: frontend
    app.kubernetes.io/part-of: sentinelopsai
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
